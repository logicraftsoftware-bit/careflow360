import { Router } from 'express'; import { z } from 'zod'; import { asyncRoute, audit, auth, ok, prisma, tenantId, AppError } from '../lib.js';
export const crmRouter=Router(); crmRouter.use(auth);
const resources:any={branches:prisma.branch,departments:prisma.department,doctors:prisma.doctor,leadSources:prisma.leadSource,leads:prisma.lead,patients:prisma.patient,appointments:prisma.appointment,followups:prisma.followUp,auditLogs:prisma.auditLog,notifications:prisma.notification,supportTickets:prisma.supportTicket};
crmRouter.get('/dashboard',asyncRoute(async(req,res)=>{const tid=tenantId(req);const [leads,patients,appointments,doctors,branches,followups]=await Promise.all([prisma.lead.count({where:{tenantId:tid}}),prisma.patient.count({where:{tenantId:tid}}),prisma.appointment.count({where:{tenantId:tid}}),prisma.doctor.count({where:{tenantId:tid}}),prisma.branch.count({where:{tenantId:tid}}),prisma.followUp.count({where:{tenantId:tid,status:'PENDING'}})]);return ok(res,{leads,patients,appointments,doctors,branches,pendingFollowups:followups})}));
crmRouter.get('/modules/:module',asyncRoute(async(req,res)=>{const tid=tenantId(req);const items=await prisma.moduleRecord.findMany({where:{tenantId:tid,module:req.params.module},orderBy:{createdAt:'desc'}});return ok(res,{items,total:items.length,page:1,limit:100})}));
crmRouter.post('/modules/:module',asyncRoute(async(req,res)=>{const tid=tenantId(req);const {title,status='ACTIVE',...data}=req.body;const row=await prisma.moduleRecord.create({data:{tenantId:tid,module:req.params.module,title:title||data.name||'Untitled record',status,data}});await audit(req,`${req.params.module}.created`,'ModuleRecord',row.id);return ok(res,row,'Created successfully',201)}));
crmRouter.patch('/modules/:module/:id',asyncRoute(async(req,res)=>{const tid=tenantId(req);const found=await prisma.moduleRecord.findFirst({where:{id:req.params.id,tenantId:tid,module:req.params.module}});if(!found)throw new AppError(404,'Record not found','NOT_FOUND');const {title,status,...data}=req.body;const row=await prisma.moduleRecord.update({where:{id:found.id},data:{title:title||found.title,status:status||found.status,data:{...(found.data as object),...data}}});await audit(req,`${req.params.module}.updated`,'ModuleRecord',row.id);return ok(res,row,'Updated successfully')}));
crmRouter.delete('/modules/:module/:id',asyncRoute(async(req,res)=>{const tid=tenantId(req);const found=await prisma.moduleRecord.findFirst({where:{id:req.params.id,tenantId:tid,module:req.params.module}});if(!found)throw new AppError(404,'Record not found','NOT_FOUND');await prisma.moduleRecord.delete({where:{id:found.id}});await audit(req,`${req.params.module}.deleted`,'ModuleRecord',found.id);return ok(res,null,'Deleted successfully')}));
crmRouter.get('/:resource',asyncRoute(async(req,res)=>{const model=resources[req.params.resource];if(!model)throw new AppError(404,'Resource not found','NOT_FOUND');const page=Math.max(1,Number(req.query.page)||1),limit=Math.min(100,Number(req.query.limit)||25);const tid=tenantId(req);const [items,total]=await Promise.all([model.findMany({where:{tenantId:tid},skip:(page-1)*limit,take:limit,orderBy:{createdAt:'desc'}}),model.count({where:{tenantId:tid}})]);return ok(res,{items,total,page,limit})}));
crmRouter.post('/:resource',asyncRoute(async(req,res)=>{const model=resources[req.params.resource];if(!model)throw new AppError(404,'Resource not found','NOT_FOUND');const tid=tenantId(req);const data={...req.body,tenantId:tid};delete data.id;const row=await model.create({data});await audit(req,`${req.params.resource}.created`,req.params.resource,row.id);return ok(res,row,'Created successfully',201)}));
crmRouter.patch('/:resource/:id',asyncRoute(async(req,res)=>{const model=resources[req.params.resource];if(!model)throw new AppError(404,'Resource not found','NOT_FOUND');const tid=tenantId(req);const found=await model.findFirst({where:{id:req.params.id,tenantId:tid}});if(!found)throw new AppError(404,'Record not found','NOT_FOUND');const data={...req.body};delete data.id;delete data.tenantId;const row=await model.update({where:{id:found.id},data});await audit(req,`${req.params.resource}.updated`,req.params.resource,row.id);return ok(res,row,'Updated successfully')}));
crmRouter.delete('/:resource/:id',asyncRoute(async(req,res)=>{const model=resources[req.params.resource];if(!model)throw new AppError(404,'Resource not found','NOT_FOUND');const tid=tenantId(req);const found=await model.findFirst({where:{id:req.params.id,tenantId:tid}});if(!found)throw new AppError(404,'Record not found','NOT_FOUND');await model.delete({where:{id:found.id}});await audit(req,`${req.params.resource}.deleted`,req.params.resource,found.id);return ok(res,null,'Deleted successfully')}));
