import { PrismaClient } from '@prisma/client'; import argon2 from 'argon2'; const db=new PrismaClient();
const permissionKeys=['dashboard.view','leads.view','leads.create','leads.edit','leads.delete','leads.assign','leads.convert','appointments.view','appointments.create','appointments.edit','appointments.cancel','appointments.confirm','appointments.payment_manage','appointments.generate_token','reports.view','reports.export','staff.manage','settings.manage'];
async function main(){for(const key of permissionKeys)await db.permission.upsert({where:{key},update:{},create:{key}});const plans=[['Starter','STARTER',1499,14990,0],['Professional','PROFESSIONAL',3499,34990,1],['Enterprise','ENTERPRISE',7999,79990,2]] as const;for(const [name,code,monthlyPrice,annualPrice,sortOrder] of plans)await db.plan.upsert({where:{code},update:{},create:{name,code,description:`${name} plan for growing clinics`,monthlyPrice,annualPrice,trialDays:14,currency:'INR',sortOrder,popular:code==='PROFESSIONAL',limits:{create:[{code:'max_branches',value:code==='STARTER'?1:code==='PROFESSIONAL'?5:100},{code:'max_doctors',value:code==='STARTER'?5:code==='PROFESSIONAL'?25:500},{code:'max_staff',value:code==='STARTER'?10:code==='PROFESSIONAL'?50:1000}]}}});const email=(process.env.SUPER_ADMIN_EMAIL||'hosmedai@gmail.com').replace('\\@','@').toLowerCase();const password=process.env.SUPER_ADMIN_PASSWORD||'ChangeMe123!';let role=await db.role.findFirst({where:{tenantId:null,code:'SUPER_ADMIN'}});if(!role)role=await db.role.create({data:{name:'Super Admin',code:'SUPER_ADMIN',isSystem:true}});for(const p of await db.permission.findMany())await db.rolePermission.upsert({where:{roleId_permissionId:{roleId:role.id,permissionId:p.id}},update:{},create:{roleId:role.id,permissionId:p.id}});let user=await db.user.findFirst({where:{tenantId:null,email}});if(!user)user=await db.user.create({data:{name:'CareFlow360 Super Admin',email,passwordHash:await argon2.hash(password),isPlatform:true}});await db.userRole.upsert({where:{userId_roleId:{userId:user.id,roleId:role.id}},update:{},create:{userId:user.id,roleId:role.id}});console.log(`Seeded CareFlow360. Super admin: ${email}`)}main().finally(()=>db.$disconnect());
