services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: careflow360
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: ["careflow360_pg:/var/lib/postgresql/data"]
volumes:
  careflow360_pg:
